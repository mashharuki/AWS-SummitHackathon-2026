# @saboru/shared — Required environment variables

# SHA-256 hashing salt for pseudonymization (minimum 32 bytes random string)
# Generation: openssl rand -hex 32
PSEUDONYMIZE_SALT=

# Bedrock maximum token count (default: 8000)
MAX_TOKEN_LIMIT=8000
